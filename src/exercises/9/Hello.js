// We now expect data to be passed, as props.
// The name might not be avaible, though, so we want to fall back to something that makes sense.

import React from 'react';

function Hello() {
	return <h1>Hello World!</h1>;
}

export default Hello;
